{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Edit Article Tag' %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('tag') ~ ' Article Tags', 'article_tag_index'),
  bootstrap_breadcrumb(article_tag, 'article_tag_view', {id: article_tag.id}),
  bootstrap_breadcrumb('Edit'),
) }}
{% endblock %}
