{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'RSS Settings' %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('newspaper') ~ ' Articles', 'article_index'),
    bootstrap_breadcrumb(bootstrap_icon('gear-fill') ~ ' Settings', 'article_rss_settings'),
  ) }}
{% endblock %}
